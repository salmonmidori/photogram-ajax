var added_comment = $("<%= j(render @comment) %>");

added_comment.hide();

$("#<%= dom_id(@comment.photo) %>_<%= dom_id(@comment) %>_form").before(added_comment);

added_comment.slideDown();

$("#<%= dom_id(@comment.photo) %>_<%= dom_id(@comment) %>_form #comment_body").val("");
