$("#<%= dom_id(@follow_request.recipient) %>_follow_unfollow").replaceWith("<%= j(render "follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient ) %>")
